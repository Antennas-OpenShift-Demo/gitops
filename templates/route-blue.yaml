kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: antennas-front-blue
spec:
  to:
    kind: Service
    name: antennas-front-blue
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
