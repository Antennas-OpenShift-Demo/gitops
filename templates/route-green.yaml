kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: antennas-front-green
spec:
  to:
    kind: Service
    name: antennas-front-green
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
